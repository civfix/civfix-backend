node_modules
dist
.turbo
*.tsbuildinfo
coverage
.DS_Store
*.log

# Editor state
.idea/
.vscode/
*.swp
*~

# Env files: ignore real ones, keep the example committed.
.env
.env.*
!.env.example

# drizzle-kit diff-inspection output (db:generate). The canonical hand SQL in
# services/api/drizzle/*.sql IS tracked; only the throwaway generated copy is ignored.
**/drizzle/_generated/
